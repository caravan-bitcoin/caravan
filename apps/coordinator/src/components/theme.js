import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontSize: 14,
  },
  palette: {
    secondary: {
      main: "#e0e0e0",
    },
    primary: {
      main: "#00478E",
    },
  },
});

export default theme;
