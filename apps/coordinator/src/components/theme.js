import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontSize: 12,
  },
  palette: {
    secondary: {
      main: "#e0e0e0",
    },
    black: {
      main: "#000000",
    },
  },
});

export default theme;
